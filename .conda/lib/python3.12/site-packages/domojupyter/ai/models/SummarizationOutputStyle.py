from enum import Enum

"""
    Enum for the output style of the summarization
    
    Attributes:
        BULLETED (str): bulleted
        NUMBERED (str): numbered
        PARAGRAPH (str): paragraph
"""
class SummarizationOutputStyle(Enum):
    BULLETED = "bulleted",
    NUMBERED = "numbered",
    PARAGRAPH = "paragraph"
